<img src="assets/img/burger.jpg">

<h1>Eat The Burger</h1>

<table>
    <tr>
        <td>
          <ul>
            {{#each burger}}
              {{#unless devoured}}
                <li>
                  {{burger_name}}
                  <button class="eat-burger" data-id="{{this.id}}">Devour It!</button>
                </li>
              {{/unless}}
            {{/each}}
          </ul>
        </td>
        <td>
          <ul>
            {{#each burger}}
              {{#if devoured}}
                {{burger_name}}
              {{/if}}
            {{/each}}
          </ul>
        </td>
    </tr>
</table>

<h2>Add a Cat</h2>
<form class="create-form">

  <div class="form-group">
    <input type="text" id="burger" name="burger_name">
  </div>

  <button type="submit">Submit</button>
  <p>Enter a burger you want to it. Click the "Submit" button</p>
</form>
